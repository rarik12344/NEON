{
  "version": 2,
  "regions": ["fra1"],  // Явно указываем регион
  "functions": {
    "api/frame.js": {
      "memory": 1024,  // Минимальный объем памяти
      "maxDuration": 5  // Макс. время выполнения (сек)
    }
  }
}
